
<%= form_for @ci, :url => { :controller => "interrogator", :action => "index" }, :html => { :method => "get" } do |f| %>
  <p>
  <%= f.label :question, "Ask a Question" %>
  <%= f.text_field :question %> <%= f.error_messages %>
  </p>
  <%= f.hidden_field :slug %>
<% end %>
