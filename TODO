create models
 - Author
   -- name, string
   -- display_name, string
   -- bio, html
   -- has_many works
   -- picture, jpeg
   -- slug, string
   -- active, boolean

 - Work
   -- title, string
   -- as_by, string
   -- belongs_to author
   -- has_many chunks
   -- translator, string
   -- source, url
   -- active, boolean

 - Chunk
   -- title, string
   -- text, text
   -- belongs_to work
   -- belongs_to author 

set up indexing

set up search
   
present snippets

refine search by tokenizing the query

